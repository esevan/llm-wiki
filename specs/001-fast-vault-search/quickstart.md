# Quickstart: Fast Vault Search

1. Run `uv sync --all-extras` in the project root.
2. Run `uv run llm-wiki serve --vault /path/to/vault`.
3. Open the local URL, use Search, and optionally enable Semantic search. The first semantic
   request starts embedding generation in the background; structural results remain immediate.
