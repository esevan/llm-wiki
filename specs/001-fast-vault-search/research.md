# Research: Fast Vault Search

SQLite FTS5 supplies immediate lexical retrieval. Directory segments, filenames, frontmatter,
headings, tags, aliases, and links are stored independently so hierarchy narrows candidates
before content search. FastEmbed is optional and lazy; on this Intel macOS installation it uses
Python 3.12 with ONNX Runtime 1.22.1.
