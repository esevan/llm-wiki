> **Retired for schema 8+**: Feature completion and Problem-wide writeback routes are replaced by
> separate Task completion, Problem resolution, and exact Knowledge publication in
> [012](../../012-task-centered-workbench/contracts/application-api.md).

# API Contract

`POST /features/{id}/completion`, `/features/{id}/patches`, `/patches/{id}/apply`, `/patches/{id}/undo`, `/features/{id}/verify`, and projection/archive routes are human-triggered.
