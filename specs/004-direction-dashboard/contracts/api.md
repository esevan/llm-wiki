> **Partially superseded for schema 8+**: Goal behavior remains; Workbench/dashboard entities use the
> canonical Capture/Task/refinement contract in [012](../../012-task-centered-workbench/contracts/application-api.md).

# API Contract

`POST /goals`, `POST /problems/{id}/importance`, and `GET /dashboard` expose goals, scores, and immutable events.
