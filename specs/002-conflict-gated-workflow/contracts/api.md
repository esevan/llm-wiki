> **Retired for schema 8+**: All gate routes below are replaced by Task creation, transition,
> Problem-link, and nonblocking review routes in [012](../../012-task-centered-workbench/contracts/application-api.md).

# API Contract

`POST /captures`, `POST /captures/{id}/promote`, `POST /problems/{id}/approve`, `PUT /features/{id}/conflict`, and `POST /features/{id}/approve` enforce the state gates.
