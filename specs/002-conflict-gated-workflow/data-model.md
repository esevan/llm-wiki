# Data Model

Captures, Problems, Solutions (`features` in storage), Approvals, Conflict Reports, Work Log entries,
and validation checklist items use UUID identity. Solution conflict state is `unknown`, `conflicted`, or `clear`.
