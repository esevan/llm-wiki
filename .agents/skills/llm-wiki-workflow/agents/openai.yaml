interface:
  display_name: "LLM Wiki Workflow"
  short_description: "Bring Workbench into a natural Chat workflow"
  default_prompt: "Use $llm-wiki-workflow to briefly catch me up on my current Task and continue naturally, keeping completion and Knowledge publication separate."
dependencies:
  tools:
    - type: "mcp"
      value: "llm-wiki"
      description: "Read scoped Task context and update the shared LLM Wiki Workbench through exact reviews"
      transport: "stdio"
